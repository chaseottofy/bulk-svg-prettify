import { describe, expect, test } from '@jest/globals';

describe('example', () => {
  // write an example test that ensures 'expect' and 'test' are working
  test('example', () => {
    expect(true).toBe(true);
  });
})